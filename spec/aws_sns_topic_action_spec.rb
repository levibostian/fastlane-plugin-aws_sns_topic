describe Fastlane::Actions::AwsSnsTopicAction do
  describe '#run' do
    it 'prints a message' do
      # expect(Fastlane::UI).to receive(:message).with("The aws_sns_topic plugin is working!")

      # Fastlane::Actions::AwsSnsTopicAction.run(nil)
    end
  end
end
